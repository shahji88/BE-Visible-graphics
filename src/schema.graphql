type Query {
  userByEmail(email: String!): User
}

type Mutation {
  createUser(email: String!, password: String!, role: String!, status: String!): User!
}

type User {
  id: Int!
  email: String!
  role: String!
  status: String!
}
